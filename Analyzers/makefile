LIBRARY = Analyzers
OBJDIR = obj
DEPDIR = $(OBJDIR)/dep
SRCDIR = src
INCDIR = include
ADDITIONAL_ROOTMAPLIBRARY = -rml libAnalyzerTools.so -rml libDataFormats.so -rml libTree.so

include $(ArNeuT_WD)/makefile.common

INCLUDES += -I$(ArNeuT_WD)/DataFormats/include/
INCLUDES += -I$(ArNeuT_WD)/AnalyzerTools/include/
